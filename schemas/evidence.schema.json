{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Evidence",
  "description": "A fact, not state (ADR-002). PII-redacted BEFORE entering the event log (ADR-008 Layer 1). Signals are LLM-extracted per the extraction contract: every signal carries a verbatim quote for human spot-checking.",
  "type": "object",
  "required": ["evidence_id", "child_id", "channel", "submitted_by", "created_at"],
  "properties": {
    "evidence_id": { "type": "string" },
    "child_id": { "type": "string" },
    "mission_id": { "type": "string", "description": "Absent for free observations outside any mission." },
    "chapter_id": { "type": "string" },
    "channel": {
      "type": "string",
      "enum": ["mission_checkin", "free_observation", "child_retelling"],
      "description": "child_retelling = the child's own retelling from the return-conversation; highest signal-to-noise channel (ADR-007 §3)."
    },
    "submitted_by": { "type": "string", "enum": ["parent", "child_app"] },
    "checkin_status": {
      "type": "string",
      "enum": ["completed", "partial", "not_completed"],
      "description": "Only for channel=mission_checkin."
    },
    "raw_text": { "type": "string", "description": "PII-redacted observation text." },
    "signals": {
      "type": "array",
      "description": "May be EMPTY — pure chit-chat must extract to empty (false-positive discipline).",
      "items": {
        "type": "object",
        "required": ["target_type", "target_id", "signal_strength", "confidence", "quote"],
        "properties": {
          "target_type": { "type": "string", "enum": ["topic", "capability"] },
          "target_id": { "type": "string", "description": "mt_... topic id or capability id." },
          "signal_strength": { "type": "number", "minimum": 0, "maximum": 1 },
          "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
          "quote": { "type": "string", "description": "Verbatim substring of raw_text supporting this signal." }
        }
      }
    },
    "created_at": { "type": "string", "format": "date-time" }
  }
}
