# Next Steps

唯一权威基线：`docs/constitution.md`（Relationship First）。

## 当前：Phase 0 关系验证（进行中）

已建成的垂直闭环：Session API → Runtime JSON（Scene DSL）→ Story Player
→ 真实交互事件 → 回访/回调识别指标。

剩余：
1. **真实使用验证**（最高优先级）：让真实孩子用 7–14 天，观察
   `return_rate_d2/d7/d14` 与 callback recognition —— 这是 Phase 0
   唯一真正的验收标准。
2. 资产扩充：更多豆豆兔动作/场景 SVG（按 Character Bible 视觉规则）；
   森林背景。
3. Player 体验打磨：TTS（固定豆豆兔声音 doudou_v1）、动画节奏。
4. 防误触：家长入口与孩子入口分离（当前 preview 参数是初版）。

## 暂缓（Phase 1 门槛未达前不启动）

- Child Twin 分层激活的进一步深化（已建投影足够 Phase 0 使用）。
- Knowledge Graph（PG 表模拟起步即可，但先等回访数据）。
- 专家裁决 gate：taxonomy + topic-capability map 的 --live + 人工抽检。
- i18n 翻译覆盖（12/426 topic，回退已可见化）。
- os-taxonomy pinned submodule。
- Flutter 客户端（Web player 验证关系后再立项）。

## 已完成（不再返工）

Schema 契约体系、事件溯源与重启幂等、Safety 深模块、Capability 派生、
评估体系（黄金集 22 条 p=1.00 r=1.00）、Parent Coach、Twin 投影、
Growth Pattern/Adventure Template Library、Partner 关系记忆、
Family 兴趣桥、Scene DSL、Session 垂直闭环。
